Print a report on the status of all Git repositories in a directory.

## Build

```sh
cargo build
```

## Test

```sh
cargo test
```

## Run

```sh
cargo run -- ~/path/to/repos
```

## Install

```sh
cargo install --path .
```
